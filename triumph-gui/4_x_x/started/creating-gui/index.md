# Starting

To create a [GUI](/gui), all we need is to call the builder, configure the GUI, and then build it.
The builder allows you to specify a title, a container type, as well as add as many components as you want.  
Let's create a simple chest GUI with `1` row with an item in the middle that sends the `player` a message when clicking.

{example}

# Result

![](/static/create-gui-example.mp4)

Now that the simple GUI is created, we can dive into something more complex by using [States](/states).  
We can continue onto the next example [here](/using-states).
